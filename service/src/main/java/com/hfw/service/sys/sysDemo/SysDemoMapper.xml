<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hfw.service.sys.sysDemo.SysDemoMapper">

    <select id="page" resultType="com.hfw.model.po.sys.SysDemo">
        select *
        from sys_demo
        <where>
            deleted=0
            <if test="po.id!=null">
                and id=#{po.id}
            </if>
            <if test="po.name!=null and po.name!=''">
                and name=#{po.name}
            </if>
            <if test="page.params.name_like!=null and page.params.name_like!=''">
                and name like concat('%',#{page.params.name_like},'%')
            </if>
            <if test="po.gender!=null">
                and gender=#{po.gender}
            </if>
            <if test="po.state!=null">
                and state=#{po.state}
            </if>
            <if test="po.birth!=null">
                and birth=#{po.birth}
            </if>
            <if test="page.params.registTime_lt!=null and page.params.registTime_lt!=''">
                and regist_time &lt;= #{page.params.registTime_lt}
            </if>
            <if test="page.params.registTime_gt!=null and page.params.registTime_gt!=''">
                and regist_time &gt;= #{page.params.registTime_gt}
            </if>
            <if test="page.params.phone_like!=null and page.params.phone_like!=''">
                and phone like concat(#{page.params.phone_like},'%')
            </if>
        </where>
        <if test="page.sortByField!=null and page.sortByWay!=null">
            order by ${page.sortByField} ${page.sortByWay}
        </if>
    </select>

</mapper>